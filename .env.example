# This static portfolio currently requires no runtime secrets.
# Keep this file value-free. Add future secret key names only as placeholders.
# Store real local values in .env.local, which is ignored by Git.
